/**
 * Request/response DTOs for the account API. Records with Jakarta Bean
 * Validation constraints; kept separate from the JPA entities in
 * {@code com.eventledger.account.domain} so the wire contract and the
 * persistence model can evolve independently.
 */
package com.eventledger.account.api.dto;
