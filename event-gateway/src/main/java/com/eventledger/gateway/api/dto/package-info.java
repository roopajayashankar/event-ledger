/**
 * Request/response DTOs for the Gateway API. Records with Jakarta Bean
 * Validation constraints, kept separate from the JPA entity in
 * {@code com.eventledger.gateway.domain} so the wire contract and the
 * persistence model can evolve independently.
 */
package com.eventledger.gateway.api.dto;
